<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthydevelopers">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthyDevelopers">
        <activity android:name=".UserProfile"></activity>
        <activity android:name=".CommunityList" />
        <activity android:name=".UpdateProgress" />
        <activity android:name=".UserHabitsList" />
        <activity android:name=".AllHabitsDescription" />
        <activity android:name=".AllHabitsList" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SigninActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.HealthyDevelopersNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>